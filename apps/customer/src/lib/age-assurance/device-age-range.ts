/**
 * Phase 3 adapter: read device-reported age range signals (e.g. Play/App Store APIs).
 * Returns null until platform integrations are wired.
 */
export function getDeviceAgeRange(): null {
  return null;
}
