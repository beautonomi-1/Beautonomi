# Customer App – Dev Notes

## Animations (react-native-reanimated)

- **If animations break**, run `expo start -c` (or `pnpm start:clear`) to clear the Metro cache and restart.
- **Reanimated plugin must be last** in `babel.config.js` plugins array.
