/**
 * Zero-behavior re-export of the legacy calendar body for experiments or staged migration.
 * The calendar tab uses {@link CalendarScreen} + {@link ProviderCalendarScreen} directly.
 */
export { CalendarScreenBody as CalendarLegacyBridge } from "../../../../app/(app)/(tabs)/calendar";
