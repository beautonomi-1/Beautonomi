/**
 * Re-export the shared last-resort currency so web imports stay `@/lib/regions/...`
 * while the value is defined once in `@beautonomi/utils`.
 */
export { LAST_RESORT_CURRENCY } from "@beautonomi/utils";
