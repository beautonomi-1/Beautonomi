/**
 * Public customer booking uses the same calendar rules as the provider portal:
 * no online min-notice / max-advance policy — only real calendar constraints.
 */
export const PUBLIC_BOOKING_MIN_NOTICE_MINUTES = 0;
export const PUBLIC_BOOKING_MAX_ADVANCE_DAYS = 365;
