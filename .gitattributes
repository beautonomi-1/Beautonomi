# Cross-platform: normalize text line endings in the repo (CI/Linux + macOS + Windows).
* text=auto eol=lf

# Windows-native scripts keep CRLF when checked out on Windows.
*.{cmd,bat,ps1} text eol=crlf

# Binary assets
*.{png,jpg,jpeg,gif,webp,ico,pdf,zip,wasm,woff,woff2,ttf,eot} binary
